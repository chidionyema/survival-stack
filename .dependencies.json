{
  "_": "Every host and credential in this repository is either covered by a drill on the estate register or dismissed with a reason. drills/audit.py --ci enforces it on every pull request.",
  "scan": {"exclude": []},
  "hosts": {
    "api.cloudflare.com":                   {"covered_by": "cloudflare-gone"},
    "dash.cloudflare.com":                  {"covered_by": "cloudflare-gone"},
    "acct.r2.cloudflarestorage.com":        {"covered_by": "cloudflare-gone"},
    "survival-control-plane.REPLACE.workers.dev": {"covered_by": "cloudflare-gone"},
    "api.stripe.com":                       {"covered_by": "stripe-gone"},
    "github.com":                           {"covered_by": "github-gone"},
    "api.telegram.org":                     {"covered_by": "telegram-delivery"},
    "t.me":                                 {"covered_by": "telegram-delivery"},
    "api.hetzner.cloud":                    {"dismissed": "one of three interchangeable compute vendors this repository provisions across; losing one is what the other two are for"},
    "api.digitalocean.com":                 {"dismissed": "one of three interchangeable compute vendors this repository provisions across; losing one is what the other two are for"},
    "api.vultr.com":                        {"dismissed": "one of three interchangeable compute vendors this repository provisions across; losing one is what the other two are for"},
    "events.pagerduty.com":                 {"dismissed": "an alerting sink documented in docs/OPS_PORTAL.md; losing it loses notice of a failure, not the ability to serve, and Telegram carries the same alerts"},
    "registry.npmjs.org":                   {"dismissed": "resolved URLs recorded in package-lock.json; a build input, and the lockfile plus a cache is the exit"},
    "nodejs.org":                           {"dismissed": "printed by scripts/cf-bootstrap.sh as where a person can get node; nothing fetches it"},
    "api.ipify.org":                        {"dismissed": "used in src/cloudinit.js to learn a box's own address, and it already falls back to hostname -I on failure"},
    "www.123-reg.co.uk":                    {"dismissed": "a console URL in docs/MUMCHIMP_MIGRATION.md for a human to click; nothing calls it"},
    "icann.org":                            {"dismissed": "an EPP status code URL inside a whois fixture in test/incident-cf-permissions.test.js"},
    "opencollective.com":                   {"dismissed": "a funding URL inside package-lock.json metadata"},
    "example.com":                          {"dismissed": "a documentation example or test fixture, not a real host"},
    "ops.example.com":                      {"dismissed": "a documentation example or test fixture, not a real host"},
    "control.example.workers.dev":          {"dismissed": "a documentation example or test fixture, not a real host"},
    "your-worker-address.workers.dev":      {"dismissed": "a documentation example or test fixture, not a real host"},
    "REPLACE.workers.dev":                  {"dismissed": "a placeholder a person substitutes during setup, not a real host"},
    "REPLACE.r2.cloudflarestorage.com":     {"dismissed": "a placeholder a person substitutes during setup, not a real host"},
    "evil.example":                         {"dismissed": "a hostile host in a test fixture; the test asserts it is rejected"},
    "c.test":                               {"dismissed": "a documentation example or test fixture, not a real host"},
    "shop.test":                            {"dismissed": "a documentation example or test fixture, not a real host"}
  },
  "credentials": {
    "GITHUB_TOKEN":      {"covered_by": "github-gone"},
    "BOT_TOKEN":         {"covered_by": "telegram-delivery"},
    "HETZNER_TOKEN":     {"covered_by": "secret-rotation"},
    "DIGITALOCEAN_TOKEN":{"covered_by": "secret-rotation"},
    "DO_TOKEN":          {"covered_by": "secret-rotation"},
    "VULTR_TOKEN":       {"covered_by": "secret-rotation"},
    "JWT_SECRET":        {"covered_by": "secret-rotation"},
    "TOTP_SECRET":       {"covered_by": "secret-rotation"},
    "DB_PASSWORD":       {"covered_by": "secret-rotation"},
    "POSTGRES_PASSWORD": {"covered_by": "secret-rotation"}
  }
}
